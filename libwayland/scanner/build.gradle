plugins {
    id 'groovy-gradle-plugin'
}

gradlePlugin {
    plugins {
        scanner {
            id = 'org.freedesktop.wayland.scanner'
            implementationClass = 'org.freedesktop.Wayland.Scanner'
        }
    }
}
